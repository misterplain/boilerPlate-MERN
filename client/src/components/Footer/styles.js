const styles = {
    footerWrapper: (theme) => ({
        position: "fixed",
        bottom: "2%", 
        width: "100%", 
        display: "flex",
        flexDirection: "row",
        justifyContent: "center",

    })
}

export default styles 